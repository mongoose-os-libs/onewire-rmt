author: mongoose-os
description: Implements Mongoose rmt onewire driver
type: lib
version: 1.0

sources:
  - src
includes:
  - include

config_schema:
 - ["onewire_rmt", o, {title: "onewire_rmt settings"}]
 - ["onewire_rmt.rx_channel", i, -1, {title: "rx channel"}]
 - ["onewire_rmt.tx_channel", i, -1, {title: "tx channel"}]

tags:
  - c

build_vars:

manifest_version: 2017-09-29

